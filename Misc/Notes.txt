Nice to haves:
- Debugging mode phase that plays captured turn before generating a response (tlc)
- improve + randomise fill mode's fill

Wanna have:
- close pipeline
- fling in AI


NEXT PROMPT:

You are working in my Unity/C# Intelli-Trading 4s project.

Task:
Integrate the existing ResponsePlanner debug snapshot (LastSnapshot) into a simple Unity debug UI so I can inspect planner behaviour in real time.

Context:
- ResponsePlanner already exposes a LastSnapshot after each Plan(...) call
- The snapshot contains:
  - source descriptor summary
  - source numeric summary
  - per-response-type scores
  - selected ResponseType
  - final ResponsePlan
- I already have a debug UI system (PatternTurnDebugRenderer / TurnLoopDebugPresenter style)
- I prefer visual debugging over unit tests

Goal:
Create a lightweight, readable, real-time debug panel for the planner.

--------------------------------
REQUIREMENTS
--------------------------------

1. Create a new Unity MonoBehaviour:
   ResponsePlannerDebugRenderer

This should:
- render the planner snapshot using OnGUI (IMGUI) OR reuse existing debug UI style if appropriate
- not depend on Unity UI Toolkit or heavy UI systems
- be immediate-mode and simple

--------------------------------
2. Display content clearly

Render the following sections:

A. HEADER
- "Response Planner"
- Selected ResponseType (large / clear)

B. SOURCE SUMMARY
- Density
- Energy
- AnchorCount
- HasMeaningfulAnchors
- HasStrongEnding
- EndingIsOpen
- Profile (Front / Back / Balanced)

C. RESPONSE TYPE SCORES
For each ResponseType:
- Mirror
- Complement
- Simplify
- Intensify
- Contrast
- Fill

Display:
- score value
- highlight the selected one

D. FINAL PLAN
- TargetDensity
- ComplementarityBias
- PreserveAnchors
- MirrorEnding
- TurnLengthSteps

--------------------------------
3. Integration with existing system

Wire this into the current runtime without breaking anything:

- Access ResponsePlanner instance (via TurnLoopController or wherever it lives)
- Read planner.LastSnapshot each frame
- Do not duplicate planner logic

If needed:
- extend TurnLoopDebugPresenter OR create a parallel debug component
- keep dependencies clean and optional

--------------------------------
4. Layout and readability

- keep layout compact and aligned (grid or column-based)
- use consistent spacing
- avoid clutter
- ensure values update in real time

--------------------------------
5. Safety

- if LastSnapshot is null, display "No planner data yet"
- do not throw exceptions
- do not assume planner always ran

--------------------------------
6. Optional (nice but simple)

If easy:
- visually emphasise the selected ResponseType (e.g. prefix ">>" or change colour)
- group numeric vs boolean values cleanly

--------------------------------
CONSTRAINTS
--------------------------------

- Do not modify planner logic
- Do not add heavy UI frameworks
- Do not introduce complex architecture
- Keep this as a debug-only tool
- Follow existing project style where possible

--------------------------------
OUTPUT REQUIREMENTS
--------------------------------

1. Show the new ResponsePlannerDebugRenderer class
2. Show any minimal wiring changes (e.g. TurnLoopController or presenter)
3. Explain how to attach it in Unity (which GameObject, etc.)
4. Keep the solution simple, clean, and easy to extend later